from pathlib import Path

src = Path(__file__).parent / 'src' / '03_问题三四检验评价.md'
text = src.read_text(encoding='utf-8')
old = '此外需要注意，问题三、问题 4-3 在 $q=0.50$ 处的费用比问题二、问题 4-2 低得多，是因为其执行层为无前视贪心规则，储备下调后释放出的放电空间有限，费用上升的绝对幅度因而小一个量级；这不代表它们对储备更不敏感。'
assert text.count(old) == 1
text = text.replace(old, '上述数值仅反映当前配置下的参数敏感性。储备量不直接进入执行器，而是通过计划承诺间接影响运行；不同问题还采用不同预测配置和执行方式，因此本文不将费用增幅差异归因于某一种执行机制。')
lines = text.splitlines()
for i, line in enumerate(lines):
    if line.startswith('@@P 表 11 中 **C2 − C1**'):
        lines[i] = '@@P 表 11 中 **C2 − C1** 是保留本文策略配置时，将全知负荷与光伏换成预测序列的对照差距：问题二为 152.56 万元，问题三为 112.50 万元，问题 4-2 为 152.98 万元，问题 4-3 为 111.18 万元。同一问题的 C1、C2 使用相同执行方式；执行器与预测配置的差异存在于问题二和问题三之间，而非同问的两个档位之间。因此不能将跨问题的差额之差认定为多时点预测融合的独立收益，也不能把 110 万—113 万元称作融合的经济价值。**C3 − C2** 是在负荷与光伏仍用预测值的条件下，将全知电价换成预测电价的对照差距，问题 4-2 为 7.68 万元，问题 4-3 为 7.75 万元。这些差距仅适用于当前策略配置，不作为独立因果贡献。'
    elif line.startswith('@@P 表 11 中 C0 与 C1 两栏之差'):
        lines[i] = '@@P 表 11 中 C0 与 C1 两栏之差 **C1 − C0**（问题二 11.08 万元、问题三 8.58 万元、问题 4-2 20.72 万元、问题 4-3 18.75 万元，均为正值）同样需要限定。C0 保留储电量递推、容量上下限、充放电功率限制及终值 6000 kWh，采用全期全知物理线性规划；它将正式期初始储电量放松为 10800 kWh，并去掉链式附加费和本文的阶段决策、执行规则。C1 则保留本文策略及由一月连续运行得到的正式期初始状态。因此差额混合了初始状态放松、结算费用放松和决策执行方式变化，**不能全部归因于初始储电量的放宽**，也不能视为某个模块的独立代价。C0 只用于提供物理松弛下界参照。'
src.write_text('\n'.join(lines) + '\n', encoding='utf-8')
print('Updated three review passages in shared source.')
