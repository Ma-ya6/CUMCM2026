# 计算内核与来源快照

正式内核仅为 `opt_core.py`，由根目录 `run_year.py` 调用。

`run_single.py`、`run_stage.py` 等旧脚本为一次优化版来源快照，可能包含旧路径、旧时间映射和不同终值处理，不是本版可运行交付入口。请勿据此生成新的正式费用。

使用根目录 `project.py` 或三个“最终模型”目录的运行入口；诊断与敏感性结果分别位于 `audits/` 和 `出图数据/`。
