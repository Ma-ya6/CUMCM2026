# C题符号规定

> 本文件给出C题四个问题统一使用的符号体系。时间口径以 `../时间点与时间段统一处理说明.md` 为最高优先级依据。不得再用同一个未加说明的 $t$ 同时表示自然日物理段和附件5计划段。

## 0. AI执行约束

1. 自然日物理时间轴一律用 $i$；附件5计划行时间轴一律用 $j$。
2. $i,j$ 均为从1开始的数学编号；Python零基下标必须写成 `physical_slot0`、`plan_slot0`。
3. 输入点 `00:10` 是物理段 $I_{d,1}$ 的右端点，表示00:00—00:10。
4. 第 $d$ 日计划段 $J_{d,1}$ 是00:10—00:20，不能与输入第1列直接配对。
5. 所有带 $i$ 的实际量属于自然日执行轴；所有带 $j$ 的计划、调整和附件5计划行量属于计划轴。
6. 决策只能使用决策时刻可得信息；实际数据只能在对应物理段结束后用于执行和后续决策。
7. 若旧程序或论文继续用单一符号 $t$，必须先判定它实际属于 $i$ 还是 $j$，再更名和换算。

## 1. 单位与通用记号

| 符号 | 含义 | 单位或取值 |
|---|---|---|
| $\Delta t$ | 十分钟时段长度 | $10\text{ min}=1/6\text{ h}$ |
| $T$ | 每条时间轴的十分钟段数 | 144 |
| $D$ | 2025年自然日数 | 365 |
| $d$ | 日期序号 | $1,\ldots,365$，对应2025.1.1—12.31 |
| $K$ | 决策发布时刻集合 | $\{0,6,12,18\}$，单位为时 |
| $k$ | 某次决策发布时刻 | $k\in K$ |
| $Q_q(\cdot)$ | 样本的 $q$ 分位数 | — |
| 上标 $\mathrm{fc}$ | 预测量 | forecast |
| 上标 $\mathrm{act}$ | 实际量 | actual |
| 上标 $(k)$ | 时刻 $k$ 发布的决策或预测 | $k\in K$ |

功率使用kW，电量使用kWh，电价使用元/kWh，费用使用元。若小写变量表示功率，则对应十分钟电量等于功率乘 $\Delta t$；附件5购电量、充放电量均输出kWh。

## 2. 两套时间轴

### 2.1 自然日物理时间轴

$$
\tau_{d,0}=d\text{日 }00{:}00,
\qquad
\tau_{d,i}=\tau_{d,0}+i\Delta t,
\qquad i=1,\ldots,144.
$$

物理段定义为

$$
I_{d,i}=(\tau_{d,i-1},\tau_{d,i}].
$$

| $i$ | 物理段 $I_{d,i}$ | 输入数据右端点 |
|---:|---|---|
| 1 | 00:00—00:10 | `00:10` |
| 36 | 05:50—06:00 | `06:00` |
| 37 | 06:00—06:10 | `06:10` |
| 72 | 11:50—12:00 | `12:00` |
| 108 | 17:50—18:00 | `18:00` |
| 144 | 23:50—次日00:00 | `0:00+1` |

附件1、2、4中的实际或给定数据 $L_{d,i}$、$P_{d,i}$、$c_{d,i}$ 均按该轴解释。

### 2.2 附件5计划行时间轴

$$
J_{d,j}
=
(d\text{日 }00{:}10+(j-1)\Delta t,\ d\text{日 }00{:}10+j\Delta t],
\qquad j=1,\ldots,144.
$$

| $j$ | 计划段 $J_{d,j}$ |
|---:|---|
| 1 | 当日00:10—00:20 |
| 35 | 当日05:50—06:00 |
| 36 | 当日06:00—06:10 |
| 72 | 当日12:00—12:10 |
| 108 | 当日18:00—18:10 |
| 143 | 当日23:50—次日00:00 |
| 144 | 次日00:00—00:10 |

## 3. 两套时间轴的唯一映射

定义计划段到物理段的映射 $\pi$：

$$
\pi(d,j)=
\begin{cases}
(d,j+1),&j=1,\ldots,143,\\
(d+1,1),&j=144.
\end{cases}
$$

因此 $J_{d,j}=I_{\pi(d,j)}$。对任一自然日物理量 $Z\in\{L,P,c\}$，定义计划轴对应量

$$
\widetilde Z_{d,j}=Z_{\pi(d,j)}.
$$

反向映射 $\psi$ 为

$$
\psi(d,i)=
\begin{cases}
(d-1,144),&i=1,\\
(d,i-1),&i=2,\ldots,144.
\end{cases}
$$

即自然日 $d$ 的执行顺序固定为

$$
J_{d-1,144},J_{d,1},\ldots,J_{d,143}.
$$

不得使用 $Z_{d,j}$ 与计划 $x_{d,j}$ 同列硬配；执行结算必须先通过 $\pi$ 或 $\psi$ 转换。

## 4. 负载、光伏与电价

| 符号 | 含义 | 单位 |
|---|---|---|
| $L_{d,i}^{\mathrm{act}}$ | 物理段 $I_{d,i}$ 的实际负载代表功率 | kW |
| $P_{d,i}^{\mathrm{act}}$ | 物理段 $I_{d,i}$ 的实际可发光伏功率 | kW |
| $c_{d,i}^{\mathrm{act}}$ | 物理段 $I_{d,i}$ 的实际电价 | 元/kWh |
| $\widehat L_{d,j\mid k}$ | 时刻 $k$ 对计划段 $J_{d,j}$ 的负载预测 | kW |
| $\widehat P_{d,j\mid k}$ | 时刻 $k$ 对计划段 $J_{d,j}$ 的十分钟光伏预测 | kW |
| $\widehat c_{d,j\mid k}$ | 时刻 $k$ 对计划段 $J_{d,j}$ 的电价预测 | 元/kWh |
| $N_{d,i}^{\mathrm{act}}$ | 实际净负荷，$L_{d,i}^{\mathrm{act}}-P_{d,i}^{\mathrm{act}}$ | kW |
| $\widehat N_{d,j\mid k}$ | 预测净负荷，$\widehat L_{d,j\mid k}-\widehat P_{d,j\mid k}$ | kW |
| $F_{d,k,h}$ | 第 $d$ 日时刻 $k$ 发布的未来第 $h$ 小时整点光伏点预测 | kW |

$F_{d,k,h}$ 对应绝对时刻 $d\text{日 }k{:}00+h\text{小时}$，$h=1,\ldots,24$，不是小时区间量。十分钟光伏预测采用时间说明规定的分段线性插值。零小时锚点固定为：00:00取前一日`0:00+1`，06:00/12:00/18:00取当日同名输入点；2025年1月1日00:00取0。

00:00初始计划的 $J_{d,144}$ 超出附件3整点预测范围10分钟，固定

$$
\widehat P_{d,144\mid0}=0.
$$

负载和电价不得随之置0，必须使用相应因果预测或已知固定价表。

## 5. 储能参数和状态

| 符号 | 含义 | 取值或单位 |
|---|---|---|
| $E_{d,i}$ | 执行完物理段 $I_{d,i}$ 后的SOC | kWh |
| $E_{d,0}$ | 自然日 $d$ 的00:00 SOC | kWh |
| $E_{\mathrm{cap}}$ | 额定容量 | 12000 kWh |
| $E_{\min},E_{\max}$ | SOC上下限 | 1200、10800 kWh |
| $P_{\mathrm{st}}^{\max}$ | 最大充/放电功率 | 5000 kW |
| $\eta_c,\eta_d$ | 充、放电效率 | 0.90、0.90 |
| $R_d$ | 第 $d$ 日安全储备 | kWh；若模型启用，须说明估计方法 |

$$
E_{d,i}=E_{d,i-1}+\eta_cu_{d,i}\Delta t-\frac{v_{d,i}\Delta t}{\eta_d}.
$$

日循环约束按自然日施加：

$$
E_{d,144}=E_{d,0}.
$$

用计划轴表达时，该等式位于执行完 $J_{d,143}$ 后，不能移到 $J_{d,144}$ 后。

## 6. 计划、调整与最终承诺

| 符号 | 含义 | 单位 |
|---|---|---|
| $x_{d,j}^{(0)}$ | 第 $d$ 日00:00为 $J_{d,j}$ 制定的计划购电功率 | kW |
| $x_{d,j}^{(6)},x_{d,j}^{(12)},x_{d,j}^{(18)}$ | 相应时刻发布的调整后购电功率 | kW |
| $x_{d,j}^{\mathrm{final}}$ | $J_{d,j}$ 开始前最后一次已发布的有效承诺 | kW |
| $X_{d,j}^{(k)}$ | 附件5输出购电量，$x_{d,j}^{(k)}\Delta t$ | kWh |

零延迟假设下：

$$
\mathcal J_0=\{1,\ldots,144\},\quad
\mathcal J_6=\{36,\ldots,144\},
$$

$$
\mathcal J_{12}=\{72,\ldots,144\},\quad
\mathcal J_{18}=\{108,\ldots,144\}.
$$

$$
k^*(d,j)=\max\{k\in K:j\in\mathcal J_k,\ x_{d,j}^{(k)}\text{ 已发布}\},
\qquad
x_{d,j}^{\mathrm{final}}=x_{d,j}^{(k^*(d,j))}.
$$

若某时刻没有为该段发布新值，则沿用上一次有效承诺。

## 7. 自然日执行变量

$$
x_{d,i}^{\mathrm{exec}}=
\begin{cases}
x_{d-1,144}^{\mathrm{final}},&i=1,\\
x_{d,i-1}^{\mathrm{final}},&i=2,\ldots,144.
\end{cases}
$$

| 符号 | 含义 | 单位或约束 |
|---|---|---|
| $y_{d,i}$ | 实际使用的承诺购电功率 | $0\le y_{d,i}\le x_{d,i}^{\mathrm{exec}}$，kW |
| $r_{d,i}$ | 未使用的承诺购电功率 | $x_{d,i}^{\mathrm{exec}}-y_{d,i}$，kW |
| $g_{d,i}$ | 实际使用光伏功率 | $0\le g_{d,i}\le P_{d,i}^{\mathrm{act}}$，kW |
| $w_{d,i}$ | 弃光功率 | $P_{d,i}^{\mathrm{act}}-g_{d,i}$，kW |
| $u_{d,i}$ | 储能充电功率 | $0\le u_{d,i}\le P_{\mathrm{st}}^{\max}$，kW |
| $v_{d,i}$ | 储能向负载供给的放电功率 | $0\le v_{d,i}\le P_{\mathrm{st}}^{\max}$，kW |
| $e_{d,i}$ | 紧急购电功率 | $e_{d,i}\ge0$，kW |

$$
y_{d,i}+g_{d,i}+v_{d,i}+e_{d,i}=L_{d,i}^{\mathrm{act}}+u_{d,i}.
$$

输出十分钟电量时，上述功率均乘 $\Delta t$。

## 8. 调整量与费用符号

对一次有效调整 $k$，令 $k^-$ 为该段上一次实际发布承诺的时刻：

$$
\Delta_{d,j}^{-(k)}=\max\{x_{d,j}^{(k^-)}-x_{d,j}^{(k)},0\},
$$

$$
\Delta_{d,j}^{+(k)}=\max\{x_{d,j}^{(k)}-x_{d,j}^{(k^-)},0\}.
$$

| 符号 | 含义 | 单位 |
|---|---|---|
| $C_d^{\mathrm{planrow}}$ | 第 $d$ 日附件5计划行144段合计费用，00:10—次日00:10 | 元 |
| $C_d^{\mathrm{execday}}$ | 自然日00:00—24:00实际执行费用 | 元 |
| $C_d^{\mathrm{emer}}$ | 自然日紧急购电费用 | 元 |
| $C_d^{\mathrm{adj}}$ | 调整产生的违约和超购费用 | 元 |
| $C_d$ | 按对应问题定义的总费用 | 元 |
| $\lambda_E$ | 终端储能价值系数；如使用须注明对应SOC时刻 | 元/kWh |

计划段价格使用 $\widetilde c_{d,j}=c_{\pi(d,j)}$。紧急购电价为对应物理段电价的5倍，下调违约部分按0.5倍、上调超购部分按1.5倍计价。

不得使用未注明边界的“每日费用”或 `daily_total`；必须说明是计划行合计还是自然日执行合计。

## 9. 自然日储能输出映射

| 附件5储能区间 | 对应最终执行段 |
|---|---|
| 00:00—04:00 | $J_{d-1,144}$ 与 $J_{d,1:23}$ |
| 04:00—08:00 | $J_{d,24:47}$ |
| 08:00—12:00 | $J_{d,48:71}$ |
| 12:00—16:00 | $J_{d,72:95}$ |
| 16:00—20:00 | $J_{d,96:119}$ |
| 20:00—24:00 | $J_{d,120:143}$ |

附件5的00:00储电量为 $E_{d,0}$，24:00储电量为 $E_{d,144}$。第 $d$ 日计划的 $J_{d,144}$ 属于次日，不能计入第 $d$ 日储能表。

## 10. 首尾边界

### 10.1 2025年1月1日

$$
E_{1,0}=6000\text{ kWh},
\qquad
x_{1,1}^{\mathrm{exec}}=0.
$$

$x_{1,1}^{\mathrm{exec}}$ 指1月1日00:00—00:10冷启动段计划购电功率。缺口按光伏、储能和紧急购电规则处理。1月作为问题2—4预运行期，2月1日不重新初始化SOC。

### 10.2 问题1典型日

问题1按周期稳态处理：上一周期第144计划段等同当前周期第144计划段，自然日执行仍为“上一周期第144段 + 当前周期第1—143段”，并满足 $E_{144}=E_0$。

### 10.3 2025年12月31日

12月31日计划第144段属于2026年1月1日00:00—00:10，应保留计划输出；2025自然日实际统计在执行完12月31日第143计划段时结束。没有2026年实际数据时，不得回填该段实际值。

## 11. Python下标对照

| 物理含义 | 数学编号 | Python零基下标 |
|---|---:|---:|
| 自然日06:00—06:10 | $i=37$ | `physical_slot0=36` |
| 计划行06:00—06:10 | $j=36$ | `plan_slot0=35` |
| 自然日12:00—12:10 | $i=73$ | `physical_slot0=72` |
| 计划行12:00—12:10 | $j=72$ | `plan_slot0=71` |
| 自然日18:00—18:10 | $i=109$ | `physical_slot0=108` |
| 计划行18:00—18:10 | $j=108$ | `plan_slot0=107` |

```python
PHYSICAL_STAGE_START = {0: 0, 6: 36, 12: 72, 18: 108}
PLAN_STAGE_START = {0: 0, 6: 35, 12: 71, 18: 107}
```

## 12. 最终检查

- [ ] 实际量只使用 $i$，计划与调整只使用 $j$；
- [ ] $J_{d,1}$ 映射到 $I_{d,2}$，$J_{d,144}$ 映射到 $I_{d+1,1}$；
- [ ] 06:00调整从 $j=36$ 开始，而物理数组从 $i=37$ 开始；
- [ ] 最终承诺取该段开始前含边界时刻最后一次已发布值；
- [ ] 附件3整点预测经线性插值后才成为十分钟预测；
- [ ] 日循环约束位于执行完 $J_{d,143}$ 后；
- [ ] 自然日储能表为前一日第144段加当日前143段；
- [ ] 1月1日冷启动购电为0，2月1日SOC不重置；
- [ ] 计划行合计与自然日执行合计未混用。
