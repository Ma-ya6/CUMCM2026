> 当前支撑材料说明：本文件说明问题1代码。所有命令在支撑材料根目录执行；原始输入在 C题/附件/，问题1成品结果已收录，求解图与检验图可按本说明生成。完整交付步骤以根目录README和复现说明为准。

# C 题 问题一 · 约束 A / 约束 B 合并求解

一个求解脚本、一套结果、一套图、一套检验。两个约束口径写在同一个脚本的两个线性规划
函数里，`main()` 读一次数据、先后调用，结果写进同一个表格文件的两张 sheet。

---

## 一、目录

问题一的代码、结果、图、检验按项目统一结构分开放，不集中在一个文件夹里：

```
代码/问题1/
├── solve_q1_ab.py            求解脚本（solve_a / solve_b 两个 LP 函数 + main）
├── plot_q1.py                出图脚本（三张图：负载光伏净负荷 / 储能 / 计划购电量）
├── plot_style.py             全项目共用的配色与坐标轴样式
├── 一键运行.cmd              等价于 python solve_q1_ab.py
└── README.md

结果/问题1/
├── 问题一结果_约束AB.xlsx          主交付：三张 sheet（约束A / 约束B / 汇总）
├── 约束A（固定）_完整结果.csv      145 行逐时段明细，供出图脚本读取
├── 约束B（自由）_完整结果.csv      同上
├── result1_约束A（固定）.xlsx      题目模板格式
├── result1_约束B（自由）.xlsx      题目模板格式
└── 预处理表.csv                    附件 1 整理后的标准表（145 行 × 8 列）

图表/问题1/
├── 求解图/约束A（固定）/  图1 负载光伏净负荷、图2 储能SOC与充放电功率、图3 计划购电量
├── 求解图/约束B（自由）/  同名三张
└── 检验图/                图1 对偶变量与边际电价（A/B 各一张）、图2 参数敏感性

验证/代码/问题1/            check_model.py、check_optimal.py、sensitivity.py、tableio.py
验证/数据/                  模型检验表.xlsx、最优性检验.xlsx、敏感性结果.xlsx
```

检验脚本的判据与结论见 [验证/代码/问题1/README.md](../../验证/代码/问题1/README.md)。

---

## 二、两个约束

目标函数与约束 (1)(2)(3)(5) 完全相同，唯一差别是约束 (4) 日末回归：

| | 约束 A（固定） | 约束 B（自由） |
|---|---|---|
| 约束 (4) | E_0 = E_T = 6000 | E_T − E_0 = 0，共同取值由优化决定 |
| 含义 | 日循环两端钉死在 6000 kWh | 把这一天看成纯粹循环调度周期，循环电量自己定 |
| 代价 | 计划起点与实际初始状态绑定 | 起点可能与实际 6000 kWh 不一致，执行时要先补差额 |

约束 A 的任意可行解在 B 中都可行，所以 B 的可行域更大，必有 C(B) ≤ C(A)。

---

## 三、结果

| 指标 | 约束 A（固定） | 约束 B（自由） | 差值 |
|---|---|---|---|
| 全天购电量 Q (kWh) | 59482.6990 | 59482.6987 | −0.0003 |
| 全天购电费 C (元) | 35126.9486 | 35118.5986 | −8.35 |
| 优化得到的循环储电量 E_0 = E_T (kWh) | 6000 | 8550 | +2550 |
| 充电 ΣuΔt / 放电 ΣvΔt (kWh) | 20740.6661 / 16799.9396 | 同左 | 0 |
| 弃光 (kWh) | 0 | 0 | 0 |
| 无储能基准购电费 (元) | 48052.0466 | 48052.0466 | 0 |
| 储能节省 (元) | 12925.0980 | 12933.4480 | +8.35 |

（购电量、充放电量三个数由 `结果/问题1/约束A（固定）_完整结果.csv` 与
`约束B（自由）_完整结果.csv` 逐时段求和核对过；其余取自 `问题一结果_约束AB.xlsx` 汇总 sheet。）

两个口径购电量、充放电量几乎完全相同，只差起点储电量；把循环电量从 6000 抬到 8550 只省
8.35 元（0.024%），说明日循环电量对购电费几乎没有影响 —— 费用差异完全来自电价时段
而非储电量水平。

---

## 四、复现步骤

### 环境

依赖统一写在项目根的 `requirements.txt`：`numpy`、`pandas`、`scipy`、`scikit-learn`、
`lightgbm`、`matplotlib`、`openpyxl`。求解器用 scipy 自带的 HiGHS
（`scipy.optimize.linprog` 的 `method="highs"`），随 scipy 一起装，无需单独配置。
出图用 `Microsoft YaHei` / `SimHei` 中文字体，Windows 自带。

### 六步

所有命令都在项目根目录下执行。

| 步骤 | 命令 | 产出 | 用时 |
|---|---|---|---|
| 1 求解 | `python 代码/问题1/solve_q1_ab.py` | `结果/问题1/` 六个文件 | 约 2 秒 |
| 2 出图（约束A） | `python 代码/问题1/plot_q1.py "结果/问题1/约束A（固定）_完整结果.csv" "图表/问题1/求解图/约束A（固定）"` | 3 张图 | 约 1 秒 |
| 3 出图（约束B） | `python 代码/问题1/plot_q1.py "结果/问题1/约束B（自由）_完整结果.csv" "图表/问题1/求解图/约束B（自由）"` | 3 张图 | 约 1 秒 |
| 4 模型检验 | `python 验证/代码/问题1/check_model.py` | `验证/数据/模型检验表.xlsx` | < 1 秒 |
| 5 最优性检验 | `python 验证/代码/问题1/check_optimal.py` | `验证/数据/最优性检验.xlsx` + 检验图 1（A/B 各一张） | < 1 秒 |
| 6 敏感性 | `python 验证/代码/问题1/sensitivity.py` | `验证/数据/敏感性结果.xlsx` + 检验图 2 | < 1 秒 |

第 5、6 步以及第 2、3 步的 `from plot_style import ...` 由同目录的 `plot_style.py`
提供（见第七节），六步均可直接运行。

### 复现是否成功：跑完核对下表，全部对上就说明复现无误。

| 指标 | 期望值 | 在哪看 |
|---|---|---|
| 全天购电量 Q | 59482.6990 kWh（A）/ 59482.6987 kWh（B） | `问题一结果_约束AB.xlsx` 汇总 sheet |
| 全天购电费（约束A 固定） | 35126.9486 元 | 同上 |
| 全天购电费（约束B 自由） | 35118.5986 元 | 同上 |
| 两者之差 | 8.3500 元 | 同上 |
| 约束B 优化出的 E_0 = E_T | 8550 kWh | 同上 |
| 充电 / 放电总量 | 20740.6661 / 16799.9396 kWh | 同上 |
| 弃光总量 | 0 kWh | 同上 |
| 无储能基准购电费 | 48052.0466 元 | 同上 |
| 储能节省 | 12925.0980 元（26.90%） | 同上 |
| 充放电量比 Σv/Σu | 0.810000 = η_c·η_d | 模型检验表第 ⑦ 组 |
| 全天能量守恒两端 | 131765.4742 kWh | 模型检验表第 ⑥ 组 |
| KKT 违反变量数 | 0（五个变量块各 0） | 最优性检验表 |
| 对偶目标 | 与原始目标相等，间隙 7.3e-12 元 | 最优性检验表 |
| 敏感性结果表行数 | 15 行数据（基准 1 + 效率 6 + 容量 7 + 松绑 1） | 敏感性结果表；功率扫描三个数写在表注，脚本总共重解 19 次 |

---

## 五、可以改的地方

| 想改什么 | 改哪 |
|---|---|
| 储电量上下限、充放电功率上限、充放电效率、约束 A 的固定端点 | `solve_q1_ab.py` 顶部的 `E_MIN`、`E_MAX`、`P_ST_MAX`、`ETA_C`、`ETA_D`、`E_ENDPOINT` |
| 敏感性扫描的取值点 | `验证/代码/问题1/sensitivity.py` 里的 `eta_c_pts`、`eta_d_pts`、`emax_pts`、`pst_pts` |
| 图的配色 | `plot_q1.py` 顶部的颜色常量 |
| Excel 表头底色、列宽上限 | `验证/代码/问题1/tableio.py` 顶部的 `HEADER_FILL` 与 `write_table` 里的列宽那一行 |

改完记得把 `结果/问题1/`、`图表/问题1/` 重新跑一遍，检验那三个脚本也要跟着重跑，
否则表和图会对不上。

---

## 六、固定约定

- 一天 144 个十分钟时段，`Δt = 1/6 h`。时间列统一折算成距 0 点的分钟数，附件 1 末尾的
  `0:00+1` 单独处理。
- 附件 1 是全年同日刻的平均值（三列与"按同时刻取平均"的相关系数均为 1.0000），所以它
  对应的是一条典型日曲线，不是某一天。
- 不允许光伏上网，富余只能充电或弃光；本解中弃光为 0。
- 求解得到的对偶向量长度等于等式约束条数（约束 A 是 290），前 144 个对应各时段的电量
  平衡约束。

---

## 七、关于 `plot_style.py`

`plot_q1.py`（第 43 行）、`验证/代码/问题1/check_optimal.py` 与 `sensitivity.py` 都要
`from plot_style import ...`。该模块不在任何一份源材料里；本仓库把它收进
`代码/问题1/plot_style.py`，提供八个颜色常量与
`apply_base_style()`、`style_frame_grid()` 两个函数。

取这个位置是因为：`plot_q1.py` 与本模块同目录，导入自己所在目录即可；
两个验证脚本运行时会显式把 `代码/问题1/` 插入 `sys.path`，也指向此处。
`代码/共享/出图数据/plot_result_figures.py` 同样复用这一份样式。

因此问题1 的全部产物（六个结果文件、三张求解图、三张检验图、三张检验表）
都能由本仓库代码重新生成。

配色沿用下表，后续问题若要新增问题一的图请保持同一套：

| 色值 | 角色 | 用在哪 |
|---|---|---|
| `#005792` | 主色 | 净负荷、计划购电量、对偶变量、储电量 SOC 曲线（alpha 0.8） |
| `#00A3FF` | 次色 | 小区负载 |
| `#009473` | 第四档 | 光伏、充电功率 |
| `#E57373` | 浅红 | 放电功率（alpha 0.42） |
| `#9AA4B2` | 参考线 | 零线、SOC 上下限、基准线 |
